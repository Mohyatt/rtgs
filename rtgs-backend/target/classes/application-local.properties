# PostgreSQL local (docker-compose à la racine du dépôt : docker compose up -d)
spring.datasource.url=jdbc:postgresql://localhost:5433/rtgs
spring.datasource.username=postgres
spring.datasource.password=postgres
